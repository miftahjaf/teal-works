using UnityEngine;
using System.Collections;

namespace TexDrawLib
{
    /*public class TexMetaTag : IFlushable
    {
        public static TexMetaTag Get(TexChar Char, float Height, float Depth, float Bearing, float Italic, float Width, float Scale)
        {
            var metric = ObjPool<TexCharMetric>.Get();
            metric.ch = Char;
            metric.height = Height * Scale;
            metric.depth = Depth * Scale;
            metric.bearing = Bearing * Scale;
            metric.italic = Italic * Scale;
            metric.width = Width * Scale;
            return metric;
        }
        float posX;
        float posY;
        float sizeX;
        float sizeY;
        Color color;
        float rotation;
    }*/
}
